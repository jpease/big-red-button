# Enforce LF line endings on checkout for all text files
# This prevents Windows CI from converting to CRLF and confusing pre-commit
* text=auto eol=lf

# Binary files (images, etc.) should be treated as binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
